name: Luna private V3 source staging
on:
  push:
    branches: [main]
    paths: [.github/workflows/luna-stage-v3-20260924.yml]
permissions:
  contents: read
concurrency:
  group: diana-lifecycle-preflight
  cancel-in-progress: false
jobs:
  stage:
    if: github.repository == 'gomez5757/diana-luna-controller-public' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - name: Start fixed Codespace
        env:
          DIANA_CODESPACE_LIFECYCLE_TOKEN: ${{ secrets.DIANA_CODESPACE_LIFECYCLE_TOKEN }}
        run: |
          python3 -c "import controller; controller.api_post('start')"
      - name: Verify sandbox and App Server metadata without model turns
        env:
          GH_TOKEN: ${{ secrets.DIANA_CODESPACE_LIFECYCLE_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          timeout --kill-after=10s 360s gh codespace ssh -c diana-luna-secure-auth-pjxq7jrj7xpwfrpjx -- 'bash -lc '\''set -euo pipefail; cd /workspaces/.diana-luna-control-v3; git -c core.hooksPath=/dev/null fetch -q origin fe53525f23f5811dc721b28c86ab0176d58e5028; git show fe53525f23f5811dc721b28c86ab0176d58e5028:infra/luna-codespace/parallel_v3/recovery/fix_helper_visibility.py | python3 - fe53525f23f5811dc721b28c86ab0176d58e5028'\'''
      - name: Confirm provider shutdown
        if: always()
        env:
          DIANA_CODESPACE_LIFECYCLE_TOKEN: ${{ secrets.DIANA_CODESPACE_LIFECYCLE_TOKEN }}
        run: python3 controller.py --stop
